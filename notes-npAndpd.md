```
t5=np.arange(12).reshape((3,4))   3行4列数组
t5=flatten()   横向展开
t5=np.arange(12,dtype=类型)
np.sctypeDict.keys()    查看所有数据类型的字符码

切片与列表操作相同

计算：在任何方向上的维度都不同时，就不能计算，否则可以计算

t1=np.loadtxt('./data/GB_video_data_numbers.csv',d
type=np.int64,delimiter=',')
取不连续的多行：t1[[2,8,10]]      2,8,10行
通用方法：t1[2,3]
	t1[:,0]                                     第一列
	t1[:,2:]                                     第三列后的每一列
	t1[:,[0,2]]                                  第一列和第三列
	t1[2:5,1:4]                                 第三行到第五行，第2列到第4列
	t1[[0,2，。。。],[0,1，。。。]]    取多个不相邻的点（0,0），（2,1）位置
                t1[t1<3]=10                              把小于3的替换为10 (布尔索引)
	
	np.where(t1<10,0,10)               把t1小于10的赋值为0，其余的为10
	np.clip(10,18)                           小于10的替换成10，大于18的替换成18
	t.T			转置
	t.transpose()		转置
	t.swapaxes()		转置

t[[1,2],:]=t[[2,1],:]   第二行与第三行交换
t[:,[0,2]]=t[:,[2,0]]   第一列与第三列交换

创建一个全为0的数组: np.zeros((3,4))
创建一个全为1的数组:np.ones((3,4))
创建一个对角线为1的正方形数组(方阵):np.eye(3)
np.argmax()   最大值的位置
np.argmin()   最小值的位置

 .copy() 深拷贝
np.loadtxt()     读取本地文件

np.nan!=np.nan       ======>  np.isnan(t1)
np.count_nonzero(np.nan!=np.nan)

两个pandas要对对应坐标的元素相加，可以转为numpy进行矩阵运算
思路：1.想要反映出什么样的结果，解决什么问题
          2.选择什么样的呈现方式
          3.数据还需要什么样的处理
          4.写代码

```